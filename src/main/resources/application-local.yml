spring:
  datasource:
    url: jdbc:mysql://localhost:3307/myapp_db
    username: admin
    password: password123
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

server.port: 8085
management:
  endpoints:
    web:
      exposure:
        include: mappings
spring:
  datasource:
    url: jdbc:mysql://localhost:3307/myapp_db
    username: admin
    password: password123
  jps:
    show-sql: true
    hibernate:
      ddl-auto: update
  profiles.active: local


management:
  endpoints:
    web:
      exposure:
        include: mappings
springdoc:
  swagger-ui:
    path: "/swagger-ui.html"